import {containsCommonItem3} from "./common_items_of_array.js";
import {hasPairWithSum2} from './has_pair_with_sum_google.js';

const array1 = ['a', 'b', 'c', 'x'];
const array2 = ['z', 'a', 'y'];

const arr = [1, 2, 3, 4, 5, 6];
const sum = 5;

console.log(hasPairWithSum2(arr, sum));

console.log(containsCommonItem3(array1, array2));


